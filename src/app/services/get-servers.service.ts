/*
import { Injectable } from '@angular/core';
import {HttpClient} from '@angular/common/http';

@Injectable({
  providedIn: 'root'
})
export class GetServersService {

  public url = '/get-servers';

  constructor(private http: HttpClient) { }

  getServers() {
    return this.http.get(this.url);
  }
}
*/
